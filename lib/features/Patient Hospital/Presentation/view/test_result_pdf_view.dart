import 'package:flutter/material.dart';

class TestResultPdfView extends StatelessWidget {
  const TestResultPdfView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}